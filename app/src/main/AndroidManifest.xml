<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devs.jeric.acupoints">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Stomachache_Activity" />
        <activity android:name=".Headache_Activity" />
        <activity android:name=".ColdsActivity" />
        <activity android:name=".InsomniaActivity" />
        <activity android:name=".NauseaActivity" />
        <activity android:name=".StressActivity" />
        <activity android:name=".HypertensionActivity" />
        <activity android:name=".MensCrampsActivity"></activity>
    </application>

</manifest>